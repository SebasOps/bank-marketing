# Dependencias SOLO para desarrollo/pruebas — no van dentro de la imagen Docker final,
# para no aumentar el tamaño del contenedor con herramientas que no se usan en producción.

-r requirements.txt
pytest==8.3.3
httpx==0.27.2
pandas==2.3.3
scikit-learn==1.7.2